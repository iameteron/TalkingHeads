from setuptools import setup, find_packages

setup(
    name="oracle",
    version="0.1.0",
    description="CrafTextOracle project",
    packages=['oracle'],
    python_requires=">=3.9",
    install_requires=["PyYAML>=6.0"],
)
